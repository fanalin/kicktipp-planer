{
  "database": {
    "rules": "database.rules.json"
  },
  "hosting": {
    "public": "dist/src",
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
